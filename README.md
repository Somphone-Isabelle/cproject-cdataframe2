# cproject-cdataframe2

For windows user:
    Install Cygwin64 Terminal in order to compile and execute the code.
        To compile it:
            -$ gcc *.c -o main.exe
        To execute it:
            -$ ./main.exe
